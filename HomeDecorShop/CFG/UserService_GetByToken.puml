@startuml UserService_GetByToken
skinparam activity {
  BackgroundColor #F8F8F8
  BorderColor #333333
}

start
:user = FindUserByToken(token);
if (user is null?) then (yes)
  :return null;
  stop
else (no)
  :return MapUser(user);
  stop
endif
@enduml
