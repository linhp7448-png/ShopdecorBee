@startuml CartService_GetCurrent
skinparam activity {
  BackgroundColor #F8F8F8
  BorderColor #333333
}

start
:user = RequireUser(token);
:cart = cartRepository.GetByUserId(user.UserId);

if (cart is null?) then (yes)
  :Khởi tạo thực thể Cart mới
  - Gán UserId = user.UserId
  - Thiết lập CreatedAt và UpdatedAt = DateTime.UtcNow
  - Khởi tạo danh sách Items trống
  - Gọi cartRepository.Create(cart);
endif

:return MapCart(cart);
stop
@enduml